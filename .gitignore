# Node.js
**/node_modules/
**/npm-debug.log*
**/yarn-error.log*
.eslintcache

# Vite environment variables and build output
**/dist/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Database SQLite local file (so it doesn't overwrite online databases, though we can seed it)
# **/database.sqlite

# System files
.DS_Store
Thumbs.db
